- Implement build command

# 💬 Prompt Hints for Copilot

Common tasks Copilot can assist with:

### 🧠 Core Logic

- “Implement a function to patch all `.uss` files inside a bundle.”
- “Add schema validation for `config.json` fields.”
- “Write a CLI subcommand to rebuild all skins in `/skins/`.”

### 🧩 Swapper / Verification

- “Detect which bundles FM loads at runtime using `fs_trace.py`.”
- “Add backup-restore logic with progress output.”

### 🎨 GUI

- “Create a Tkinter window listing all skin folders with an Apply button.”
- “Add a text area that streams log messages from `logger.py`.”

### 🧪 Testing

- “Generate a pytest fixture for a temporary skin folder.”
- “Mock the `BundleManager` to test `apply_overrides` without real bundles.”

### 🪄 Docs / Automation

- “Generate Markdown documentation from the `SkinConfig` class.”
- “Extend `ci.yml` to run pytest and flake8.”
